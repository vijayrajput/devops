ID: devops
_schema-version: "2.1"
version: 0.0.1
modules:
  - name: devops-srv
    type: java
    path: srv
    parameters:
      memory: 1024M
    provides:
      - name: srv_api
        properties:
          url: ${default-url}
